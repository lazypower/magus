# AMD Strix Halo (gfx1151) is not on the official ROCm support matrix.
# This override tells the ROCm runtime to treat it as a supported target.
# Test 11.5.0 vs 11.5.1 on hardware — both reported working.
HSA_OVERRIDE_GFX_VERSION=11.5.1
